<nav class="navbar navbar-expand-lg navbar-absolute fixed-top bg-primary">
    <div class="container-fluid">
        <div class="navbar-wrapper">
            <div class="navbar-toggle">
              <button type="button" class="navbar-toggler">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
              </button>
            </div>
            {#{{ page_title_lable }}#}
            {#<a class="navbar-brand" href="{{ site_url() }}"></a>#}

            <ul class="navbar-nav">
                {% for module_nav_item in module_navs %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ module_nav_item.url }}" class="{{ module_nav_item.href_class }}">{{ module_nav_item.label|e }}</a>
                    </li>
                {% endfor %}
            </ul>
        </div>
        <div class="collapse navbar-collapse justify-content-end" id="navigation">
            {% if display_search == true %}
                <form>
                    <div class="input-group no-border">
                        <input type="text" value="" class="form-control" placeholder="Search...">
                        <div class="input-group-append">
                            <div class="input-group-text">
                                <i class="nc-icon nc-zoom-split"></i>
                            </div>
                        </div>
                    </div>
                </form>
            {% endif %}

            <ul class="navbar-nav">
                {% if display_magnify == true %}
                    <li class="nav-item">
                        <a class="nav-link btn-magnify" href="#">
                            <i class="nc-icon nc-layout-11"></i>
                            <p>
                                <span class="d-lg-none d-md-block">Stats</span>
                            </p>
                        </a>
                    </li>    
                {% endif %}
                {% if display_action == true %}
                    <li class="nav-item btn-rotate dropdown">
                        <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="nc-icon nc-bell-55"></i>
                            <p>
                                <span class="d-lg-none d-md-block">Some Actions</span>
                            </p>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="#">Action</a>
                            <a class="dropdown-item" href="#">Another action</a>
                            <a class="dropdown-item" href="#">Something else here</a>
                        </div>
                    </li>    
                {% endif %}    
                {% if display_setting  == true %}
                <li class="nav-item">
                    <a class="nav-link btn-rotate" href="#" id="navbarDropdownSetting" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="nc-icon nc-settings-gear-65"></i>
                        <p>
                            <span class="d-lg-none d-md-block">Account</span>
                        </p>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownSetting">
                        <a class="dropdown-item" href="{{ site_url() }}esciauth/change_password">Ganti Password</a>
                        <a class="dropdown-item" href="{{ site_url() }}esciauth/logout">Logout</a>
                    </div>
                </li>
                {% endif %}

                </ul>
            </div>
        </div>
    </nav>